/* =========================================================
   ЛОКАЛЬНЫЙ FIRA SANS EXTRA CONDENSED
   ========================================================= */

@font-face {
  font-family: "Fira Sans Extra Condensed";
  src: url("../fonts/Fira_Sans_Extra_Condensed/FiraSansExtraCondensed-Regular.ttf") format("truetype");
  font-weight: 400;
  font-style: normal;
  font-display: swap;
}

@font-face {
  font-family: "Fira Sans Extra Condensed";
  src: url("../fonts/Fira_Sans_Extra_Condensed/FiraSansExtraCondensed-Bold.ttf") format("truetype");
  font-weight: 700;
  font-style: normal;
  font-display: swap;
}

@font-face {
  font-family: "Fira Sans Extra Condensed";
  src: url("../fonts/Fira_Sans_Extra_Condensed/FiraSansExtraCondensed-Black.ttf") format("truetype");
  font-weight: 900;
  font-style: normal;
  font-display: swap;
}

/* =========================================================
   ОСНОВНЫЕ ПЕРЕМЕННЫЕ
   ========================================================= */

:root {
  --bg: #000000;
  --surface: #121212;
  --ink: #ffffff;
  --muted: #86868b;
  --accent: #ff3b30;
  --line: #262626;
}


/* =========================================================
   RESET
   ========================================================= */

* {
  box-sizing: border-box;
}

html,
body {
  margin: 0;
  padding: 0;
}

html {
  scroll-behavior: smooth;
}

body {
  font-family:
    -apple-system,
    BlinkMacSystemFont,
    "Segoe UI",
    Roboto,
    sans-serif;

  color: var(--ink);
  background: var(--bg);

  min-height: 100vh;
}


/* =========================================================
   ФОНОВЫЙ GRAIN
   ========================================================= */

.grain {
  position: fixed;

  inset: 0;

  pointer-events: none;

  z-index: 2000;

  opacity: 0.025;

  background-image:
    url("data:image/svg+xml,%3Csvg viewBox='0 0 180 180' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='.7'/%3E%3C/svg%3E");
}


/* =========================================================
   TOP BAR
   ========================================================= */

/* =========================================================
   TOP BAR
   ========================================================= */

.topbar {
  position: fixed;

  top: 0;
  left: 0;
  right: 0;

  width: 100%;
  height: 56px;

  z-index: 1000;

  display: flex;
  align-items: center;
  justify-content: flex-end;

  padding: 0 24px;

  background: rgba(10, 15, 30, 0.55);

  backdrop-filter: blur(18px);
  -webkit-backdrop-filter: blur(18px);

  border-bottom: 1px solid rgba(255, 255, 255, 0.07);

  transition:
    background 0.3s ease,
    box-shadow 0.3s ease;
}


body.scrolled .topbar {
  background: rgba(10, 10, 10, 0.84);

  box-shadow:
    0 8px 30px rgba(0, 0, 0, 0.28);
}


/* =========================================================
   КНОПКИ АВТОРИЗАЦИИ
   ========================================================= */

.auth-buttons {
  display: flex;

  align-items: center;

  gap: 10px;
}


/* =========================================================
   ВОЙТИ
   ========================================================= */

.btn-login {
  display: inline-flex;

  align-items: center;
  justify-content: center;

  min-height: 40px;

  padding: 8px 4px;

  color: #ffffff;

  text-decoration: none;

  font-size: 15px;

  font-weight: 700;

  white-space: nowrap;

  transition:
    opacity 0.2s ease,
    transform 0.2s ease;
}


.btn-login:hover {
  opacity: 0.7;
}


/* =========================================================
   РЕГИСТРАЦИЯ
   ========================================================= */

.cta {
  display: inline-flex;

  align-items: center;
  justify-content: center;

  height: 36px;

  padding: 0 16px;

  background: var(--accent);

  color: #ffffff;

  border-radius: 999px;

  text-decoration: none;

  font-size: 14px;
  font-weight: 700;

  white-space: nowrap;

  transition:
    transform 0.2s ease,
    filter 0.2s ease;
}

.cta:hover {
  transform: translateY(-1px);

  filter: brightness(1.08);
}

/* like */

.like {
  display: inline-flex;

  align-items: center;
  justify-content: center;

  height: 36px;

  padding: 0 16px;

  background: var(--accent);

  color: #2a00f9;

  border-radius: 999px;

  text-decoration: none;

  font-size: 14px;
  font-weight: 700;

  white-space: nowrap;

  transition:
    transform 0.2s ease,
    filter 0.2s ease;
}

.like:hover {
  transform: translateY(-1px);

  filter: brightness(1.08);
}

/* =========================================================
   АВАТАР
   ========================================================= */

.avatar {
  width: 34px;
  height: 34px;

  flex: 0 0 34px;

  border-radius: 50%;

  object-fit: cover;

  border: 2px solid rgba(255, 255, 255, 0.35);

  display: block;

  cursor: pointer;
}


.avatar:hover {
  transform: scale(1.06);

  border-color: rgba(255, 255, 255, 0.7);
}


/* =========================================================
   AUTH
   ========================================================= */

.auth-buttons {
  display: flex;

  align-items: center;

  gap: 12px;
}


.btn-login {
  color: #ffffff;

  text-decoration: none;

  font-weight: 700;

  transition:
    opacity 0.2s ease,
    transform 0.2s ease;
}


.btn-login:hover {
  opacity: 0.7;
}


.cta {
  display: inline-flex;

  align-items: center;

  justify-content: center;

  background: var(--accent);

  color: #ffffff;

  padding: 10px 20px;

  border-radius: 999px;

  text-decoration: none;

  font-weight: 700;

  transition:
    transform 0.2s ease,
    filter 0.2s ease;
}


.cta:hover {
  transform: translateY(-1px);

  filter: brightness(1.08);
}


/* =========================================================
   AVATAR
   ========================================================= */

.avatar {
  width: 40px;
  height: 40px;

  border-radius: 50%;

  object-fit: cover;

  border: 2px solid rgba(255, 255, 255, 0.35);

  display: block;
}


/* =========================================================
   ДИНАМИЧЕСКИЙ ЛОГОТИП
   ========================================================= */

.tennis-logo {
  position: fixed;
  top: var(--logo-top, 200px);
  left: var(--logo-left, 50%);
  z-index: 1100;
  width: max-content;
  height: 240px;
  pointer-events: none;
  white-space: nowrap;
  transform: translate3d(var(--logo-x, -50%), 0, 0);
  will-change: top, left, transform;
}

.tennis-logo-small {
  position: absolute;
  top: var(--small-top, 0px);
  left: var(--small-left, 0px);
  font-family: "Fira Sans Extra Condensed", sans-serif;
  font-size: var(--small-size, 42px);
  font-weight: 700;
  letter-spacing: var(--small-spacing, 0.5em);
  line-height: 1;
  color: #f9f8f9;
  text-transform: uppercase;
  margin-left: var(--small-margin, 0.22em);
  transform: translate3d(var(--small-x, -50%), 0, 0);
  will-change: top, left, transform, font-size, letter-spacing;
}

.tennis-logo-big {
  position: absolute;
  top: var(--big-top, 56px);
  left: var(--big-left, 0px);
  font-family: "Fira Sans Extra Condensed", sans-serif;
  font-size: var(--big-size, 230px);
  font-weight: 900;
  letter-spacing: var(--big-spacing, 0.1em);
  line-height: 0.82;
  color: #f9f8f9;
  text-transform: uppercase;
  white-space: nowrap;
  margin-top: 0;
  transform: translate3d(var(--big-x, -50%), 0, 0);
  will-change: top, left, transform, font-size, letter-spacing;
}

.tennis-logo-big span {
  color: #c92d24;
}

/*
 * Логотип больше НЕ переключается через body.scrolled.
 * Его координаты и размеры меняются непрерывно через
 * CSS-переменные, которые обновляет JavaScript.
 */

/* =========================================================
   HERO
   ========================================================= */

.hero {
  position: relative;

  min-height: 700px;

  max-width: 1200px;

  margin: 0 auto;

  padding: 5% 20px 5%;
}


.hero-content {
  text-align: center;

  max-width: 800px;

  margin: 0 auto;

  padding-top: 390px;

  margin-bottom: 60px;
}


.kicker {
  margin: 50px 0 15px;

  font-size: 14px;

  font-weight: 600;

  color: #ffffff;
}


.hero-content h1 {
  margin: 0 0 16px;

  font-size: 40px;

  line-height: 1.08;

  font-weight: 800;

  letter-spacing: -0.025em;
}


.hero-content .subtitle {
  margin: 0;

  color: var(--muted);

  font-size: 15px;

  line-height: 1.5;
}


/* =========================================================
   MAIN
   ========================================================= */

main {
  max-width: 1200px;

  margin: 0 auto;

  padding: 0 20px 60px;
}


/* =========================================================
   КАТАЛОГ
   ========================================================= */

.panel {
  background: var(--surface);
  border: 1px solid var(--line);
  background-color: #00000041;
  border-radius: 20px;

  padding: 30px;

  min-height: 200px;
}


.panel-head {
  display: flex;

  justify-content: space-between;

  align-items: center;

  margin-bottom: 20px;
}


.panel-head h2 {
  margin: 0;

  font-size: 22px;

  font-weight: 800;
}


/* =========================================================
   КНОПКА ОБНОВЛЕНИЯ
   ========================================================= */

.refresh-btn {
  border: 1px solid #333;

  background: #ffffff;

  color: #000000;

  padding: 7px 12px;

  border-radius: 6px;

  cursor: pointer;

  font-size: 13px;
}


.refresh-btn:hover {
  background: #eaeaea;
}


/* =========================================================
   PRODUCTS GRID
   ========================================================= */

.products-grid {
  display: grid;

  grid-template-columns:
    repeat(auto-fill, minmax(220px, 1fr));

  gap: 18px;
}


/* =========================================================
   PRODUCT CARD
   ========================================================= */

.card {
  overflow: hidden;

  background: #181818;

  border: 1px solid #292929;

  border-radius: 16px;

  cursor: pointer;

  transition:
    transform 0.2s ease,
    border-color 0.2s ease,
    box-shadow 0.2s ease;
}


.card:hover {
  transform: translateY(-4px);

  border-color: #3a3a3a;
  
  box-shadow:
    0 12px 30px rgba(255, 0, 0, 0.25);
}


/* =========================================================
   ФОТО КАРТОЧКИ
   ========================================================= */

.card-image {
  width: 100%;

  aspect-ratio: 1 / 1;

  object-fit: cover;

  display: block;

  background: #222;
}


/* =========================================================
   ТЕЛО КАРТОЧКИ
   ========================================================= */

.card-body {
  padding: 16px;
  background-color: #00000000;
}


/* =========================================================
   КАТЕГОРИЯ
   ========================================================= */

.chip {
  margin: 0 0 0px;

  color: var(--accent);

  font-size: 12px;

  font-weight: 700;
}


/* =========================================================
   НАЗВАНИЕ
   ========================================================= */

.card-title {
  margin: 0 0 7px;

  font-size: 18px;

  font-weight: 800;

  line-height: 1.15;
}


/* =========================================================
   КОРОТКОЕ ОПИСАНИЕ
   ========================================================= */

.card-text {
  margin: 0 0 15px;

  color: var(--muted);

  font-size: 14px;

  line-height: 1.4;

  display: -webkit-box;

  -webkit-box-orient: vertical;

  -webkit-line-clamp: 2;

  line-clamp: 2;

  overflow: hidden;

  min-height: 39px;
}


/* =========================================================
   ЦЕНА + НАЛИЧИЕ
   ========================================================= */

.card-row {
  display: flex;

  justify-content: space-between;

  align-items: center;

  gap: 10px;
}


.price {
  font-size: 18px;

  font-weight: 700;
}


.stock {
  color: var(--muted);

  font-size: 12px;

  text-align: right;
}


/* =========================================================
   ЕСЛИ ТОВАРОВ НЕТ
   ========================================================= */

.empty {
  margin: 20px 0;

  color: var(--muted);

  text-align: center;
}


/* =========================================================
   МОДАЛЬНОЕ ОКНО ТОВАРА
   ========================================================= */

.product-modal {
  position: fixed;

  inset: 0;

  z-index: 3000;

  display: flex;

  align-items: center;

  justify-content: center;

  padding: 20px;

  background: rgba(0, 0, 0, 0.72);

  backdrop-filter: blur(14px);

  -webkit-backdrop-filter: blur(14px);

  visibility: hidden;

  opacity: 0;

  pointer-events: none;

  transition:
    opacity 0.25s ease,
    visibility 0.25s ease;
}


.product-modal.active {
  visibility: visible;

  opacity: 1;

  pointer-events: auto;
}


/* =========================================================
   ОКНО ТОВАРА
   ========================================================= */

.product-modal-content {
  position: relative;

  width: min(680px, 100%);

  max-height: 90vh;

  overflow-y: auto;

  background: #181818;

  border: 1px solid #303030;

  border-radius: 22px;

  box-shadow:
    0 30px 90px rgba(0, 0, 0, 0.6);

  transform:
    scale(0.94)
    translateY(10px);

  transition:
    transform 0.3s cubic-bezier(0.22, 1, 0.36, 1);
}


.product-modal.active .product-modal-content {
  transform:
    scale(1)
    translateY(0);
}


/* =========================================================
   ГАЛЕРЕЯ ФОТО
   ========================================================= */

.product-modal-gallery {
  position: relative;

  width: 100%;

  height: 500px;

  display: flex;

  align-items: center;

  justify-content: center;

  background: #111;

  overflow: hidden;
}


/* =========================================================
   ФОТО В ГАЛЕРЕЕ
   ========================================================= */

.product-modal-gallery .product-modal-image {
  width: 100%;

  height: 100%;

  display: block;

  object-fit: contain;

  background: #111;
}


/* =========================================================
   СТРЕЛКИ
   ========================================================= */

.gallery-arrow {
  position: absolute;

  top: 50%;

  transform:
    translateY(-50%);

  width: 46px;

  height: 46px;

  padding: 0;

  border: none;

  border-radius: 50%;

  background:
    rgba(0, 0, 0, 0.55);

  color: #ffffff;

  font-family: Arial, sans-serif;

  font-size: 34px;

  font-weight: 300;

  line-height: 1;

  display: none;

  align-items: center;

  justify-content: center;

  cursor: pointer;

  z-index: 20;

  transition:
    background 0.2s ease,
    transform 0.2s ease;
}


.gallery-arrow:hover {
  background:
    rgba(0, 0, 0, 0.82);

  transform:
    translateY(-50%)
    scale(1.08);
}


.gallery-arrow-left {
  left: 16px;
}


.gallery-arrow-right {
  right: 16px;
}


/* =========================================================
   ИНФОРМАЦИЯ ТОВАРА
   ========================================================= */

.product-modal-body {
  padding: 26px;
}


.product-modal-title {
  margin: 0 0 10px;

  font-size: 32px;

  line-height: 1.05;

  font-weight: 800;
  position: center;
  letter-spacing: -0.02em;
}


.product-modal-description {
  margin: 0 0 0px;

  color: #a0a0a0;

  font-size: 10px;

  line-height: 0,5;
  position: center;
  white-space: pre-wrap;
}


/* =========================================================
   ЦВЕТ
   ========================================================= */

.product-modal-colors {
  margin: 0 0 20px;

  color: #a0a0a0;

  font-size: 14px;
}


/* =========================================================
   ЦЕНА / НАЛИЧИЕ
   ========================================================= */

.product-modal-info {
  display: flex;

  align-items: center;

  justify-content: space-between;

  gap: 15px;
}


.product-modal-price {
  font-size: 26px;

  font-weight: 800;
}


.product-modal-stock {
  color: #999;

  font-size: 14px;
}


/* =========================================================
   КНОПКА ЗАКРЫТИЯ
   ========================================================= */

.product-modal-close {
  position: absolute;

  top: 14px;

  right: 14px;

  z-index: 30;

  width: 40px;

  height: 40px;

  display: flex;

  align-items: center;

  justify-content: center;

  padding: 0;

  border: none;

  border-radius: 50%;

  background:
    #ffffff;

  color: #070606;

  font-size: 28px;

  line-height: 1;

  cursor: pointer;

  transition:
    background 0.2s ease,
    transform 0.2s ease;
}


.product-modal-close:hover {
  background:
    rgba(0, 0, 0, 0.9);

  transform:
    scale(1.05);
}


/* =========================================================
   МОБИЛЬНАЯ ВЕРСИЯ
   ========================================================= */

@media (max-width: 700px) {

  @media (max-width: 700px) {

  .topbar {
    height: 62px;

    padding: 0 18px;

    overflow: hidden;
  }

  .auth-buttons {
    gap: 8px;

    max-width: 100%;
  }

  .btn-login {
    font-size: 14px;

    min-height: 36px;
  }

  .cta {
    min-height: 36px;

    padding: 8px 14px;

    font-size: 13px;
  }

  .avatar {
    width: 36px;
    height: 36px;

    flex-basis: 36px;
  }

}


  /* HERO */

  .hero {
    min-height: 560px;

    padding-left: 18px;

    padding-right: 18px;
  }


  .hero-content {
    padding-top: 250px;

    margin-bottom: 40px;
  }


  .hero-content h1 {
    font-size: 30px;

    line-height: 1.1;
  }


  .hero-content .subtitle {
    font-size: 14px;
  }


  .kicker {
    font-size: 13px;

    margin: 30px 0 18px;
  }


  /* AUTH */

  .auth-buttons {
    gap: 8px;
  }


  .btn-login {
    font-size: 14px;
  }


  .cta {
    padding: 9px 15px;

    font-size: 13px;
  }


  .avatar {
    width: 36px;

    height: 36px;
  }


  /* КАТАЛОГ */

  main {
    padding-left: 18px;

    padding-right: 18px;
  }


  .panel {
    padding: 20px;

    border-radius: 16px;
  }


  .products-grid {
    grid-template-columns:
      repeat(2, minmax(0, 1fr));

    gap: 12px;
  }


  /* КАРТОЧКИ */

  .card {
    border-radius: 14px;
  }


  .card-body {
    padding: 13px;
  }


  .card-title {
    font-size: 16px;
  }


  .card-text {
    font-size: 13px;

    min-height: 36px;
  }


  .price {
    font-size: 16px;
  }


  .stock {
    font-size: 11px;
  }


  /* =====================================================
     МОБИЛЬНАЯ ГАЛЕРЕЯ
     ===================================================== */

  .product-modal {
    padding: 10px;

    align-items: center;
  }


  .product-modal-content {
    width: 100%;

    max-height: 94vh;

    border-radius: 18px;
  }


  .product-modal-gallery {
    height: 55vh;

    min-height: 300px;
  }


  .gallery-arrow {
    width: 42px;

    height: 42px;

    font-size: 30px;
  }


  .gallery-arrow-left {
    left: 10px;
  }


  .gallery-arrow-right {
    right: 10px;
  }


  .product-modal-body {
    padding: 20px;
  }


  .product-modal-title {
    font-size: 27px;
  }


  .product-modal-description {
    font-size: 14px;

    line-height: 1.55;
  }


  .product-modal-info {
    align-items: flex-start;

    flex-direction: column;

    gap: 8px;
  }


  .product-modal-price {
    font-size: 23px;
  }

}


/* =========================================================
   ОЧЕНЬ МАЛЕНЬКИЕ ЭКРАНЫ
   ========================================================= */

@media (max-width: 420px) {

  .topbar {
    padding: 0 12px;

    height: 62px;
  }


  .auth-buttons {
    gap: 6px;
  }

  

  .btn-login {
    font-size: 13px;
  }


  .cta {
    padding: 8px 12px;

    font-size: 12px;
  }


  .hero-content {
    padding-top: 220px;
  }


  .hero-content h1 {
    font-size: 27px;
  }


  .products-grid {
    grid-template-columns: 1fr;
  }


  .product-modal-body {
    padding: 18px;
  }


  .product-modal-title {
    font-size: 25px;
  }


  .product-modal-gallery {
    height: 52vh;

    min-height: 280px;
  }

}

/* =========================================================
   АККАУНТ
   Использует дизайн модального окна товара
   ========================================================= */

.account-modal-avatar {
  width: 90px;
  height: 90px;

  margin: 0 auto 22px;

  border-radius: 50%;

  overflow: hidden;

  background: #111;

  border: 1px solid #303030;
}


.account-modal-avatar img {
  width: 80%;
  height: 100%;

  display: block;

  object-fit: cover;
}


/* =========================================================
   ЗАГОЛОВОК АККАУНТА
   ========================================================= */

.account-modal-title {
  margin: 0 0 220px;

  text-align: center;
}


/* =========================================================
   ПОДЗАГОЛОВОК
   ========================================================= */

.account-modal-subtitle {
  margin: 0 0 26px;

  text-align: center;

  color: #a0a0a0;
}


/* =========================================================
   КНОПКИ
   ========================================================= */

.account-modal-actions {
  display: flex;

  flex-direction: column;

  gap: 100x;
}


.account-modal-button {
  display: flex;
  margin: 1px 0 0;
  align-items: center;

  justify-content: center;

  width: 85%;

  min-height: 48px;

  padding: 12px 18px;

  border-radius: 7px;

  text-decoration: none;

  font-weight: 700;

  transition:
    transform 0.2s ease,
    filter 0.2s ease,
    opacity 0.2s ease;
}


.account-modal-button:hover {
  transform: translateY(-1px);

  filter: brightness(1.08);
}


/* =========================================================
   КНОПКА ПРОФИЛЯ
   ========================================================= */

.account-modal-button-main {
  background: #ffffff;

  color: #000000;
}


/* =========================================================
   КНОПКА ВЫХОДА
   ========================================================= */

.account-modal-button-danger {
  background: #ff3b30;

  color: #ffffff;
}


/* =========================================================
   АККАУНТ ИСПОЛЬЗУЕТ ТОТ ЖЕ РАЗМЕР,
   ЧТО И ОКНО ТОВАРА
   ========================================================= */

#accountModal .product-modal-content {
  width: min(680px, 100%);
}


/* =========================================================
   МОБИЛЬНЫЙ АККАУНТ
   ========================================================= */

@media (max-width: 700px) {

  #accountModal .product-modal-content {
    width: 100%;
  }


  #accountModal .product-modal-body {
    padding: 20px;
  }


  .account-modal-avatar {
    width: 80px;
    height: 80px;

    margin-bottom: 20px;
  }

}

/* =========================================================
   АНИМИРОВАННЫЙ ФОН ВСЕГО САЙТА
   ========================================================= */

.abg-wr-2{
  position:fixed;
  inset:0;
  width:100%;
  height:100%;
  overflow:hidden;
  z-index:-10;
  pointer-events:none;
  background:#1a233a;
} 
.abg-2{
  position:absolute;
  inset:0;
  width:100%;
  height:100%;
  margin:0;
  padding:0;
  overflow:hidden;
  opacity:.3;
  pointer-events:none;
}
.abg-2 li{
  position:absolute;
  display:block;
  list-style:none;
  width:20px;
  height:20px;
  bottom:-150px;
  animation:animationbg-2 25s linear infinite;
}
.abg-2 li:nth-child(1){left:25%;width:80px;height:80px;animation-duration:7s;animation-delay:0s;background-image:url("../images/wallpaper/nth1.png");background-size:contain;background-repeat:no-repeat;background-position:center;}
.abg-2 li:nth-child(2){left:25%;width:80px;height:80px;animation-duration:7s;animation-delay:0s;background-image:url("../images/wallpaper/nth2.png");background-size:contain;background-repeat:no-repeat;background-position:center;}
.abg-2 li:nth-child(3){left:25%;width:80px;height:80px;animation-duration:7s;animation-delay:0s;background-image:url("../images/wallpaper/nth3.png");background-size:contain;background-repeat:no-repeat;background-position:center;}
.abg-2 li:nth-child(4){left:25%;width:80px;height:80px;animation-duration:7s;animation-delay:0s;background-image:url("../images/wallpaper/nth4.png");background-size:contain;background-repeat:no-repeat;background-position:center;}
.abg-2 li:nth-child(5){left:25%;width:80px;height:80px;animation-duration:7s;animation-delay:0s;background-image:url("../images/wallpaper/nth5.png");background-size:contain;background-repeat:no-repeat;background-position:center;}
.abg-2 li:nth-child(6){left:25%;width:80px;height:80px;animation-duration:7s;animation-delay:0s;background-image:url("../images/wallpaper/nth6.png");background-size:contain;background-repeat:no-repeat;background-position:center;}
.abg-2 li:nth-child(7){left:25%;width:80px;height:80px;animation-duration:7s;animation-delay:0s;background-image:url("../images/wallpaper/nth7.png");background-size:contain;background-repeat:no-repeat;background-position:center;}
.abg-2 li:nth-child(8){left:25%;width:80px;height:80px;animation-duration:7s;animation-delay:0s;background-image:url("../images/wallpaper/nth8.png");background-size:contain;background-repeat:no-repeat;background-position:center;}
.abg-2 li:nth-child(9){left:25%;width:80px;height:80px;animation-duration:7s;animation-delay:0s;background-image:url("../images/wallpaper/nth9.png");background-size:contain;background-repeat:no-repeat;background-position:center;}
.abg-2 li:nth-child(10){left:25%;width:80px;height:80px;animation-duration:7s;animation-delay:0s;background-image:url("../images/wallpaper/nth10.png");background-size:contain;background-repeat:no-repeat;background-position:center;}
@keyframes animationbg-2{
  0%{transform:translateY(0) rotate(0deg);opacity:1}
  100%{transform:translate(500px, -1000px) rotate(720deg);opacity:.8}
}
body{background:transparent}




/* =========================================================
   ЗАКРЫТИЕ ОКОН — АНИМИРОВАННЫЙ X
   ========================================================= */
/* =========================================================
   ФИНАЛЬНЫЙ КРЕСТИК МОДАЛЬНОГО ОКНА ТОВАРА
   ========================================================= */

.product-modal-close {
  position: absolute;
  top: 20px;
  right: 20px;

  width: 42px;
  height: 42px;

  padding: 0;
  border: none;

  background: transparent;

  cursor: pointer;
  z-index: 100;

  display: block;

  transform: rotate(0deg);
  transition: transform 0.45s ease;
}

.product-modal-close span {
  position: absolute;

  display: block;

  width: 50%;
  height: 4px;

  left: 50%;
  top: 50%;

  border-radius: 4px;

  background: #ffffff;

  transform-origin: center;

  transition: transform 0.25s ease;
}

.product-modal-close span:nth-child(1) {
  transform: translate(-50%, -50%) rotate(45deg);
}

.product-modal-close span:nth-child(2) {
  transform: translate(-50%, -50%) rotate(-45deg);
}

.product-modal-close span:nth-child(3) {
  display: none;
}

.product-modal-close:hover {
  transform: rotate(90deg);
}

@media (max-width: 700px) {

  .product-modal-close {
    top: 14px;
    right: 14px;

    width: 38px;
    height: 38px;
  }

  .product-modal-close span {
    height: 4px;
  }
}
/* =========================================================
   АККАУНТ — ВЫВЕРЕННЫЕ ОТСТУПЫ
   ========================================================= */

#accountModal .product-modal-content{
  overflow:hidden;
}
#accountModal .product-modal-body{
  padding:42px 46px 46px;
}
#accountModal .account-modal-avatar{
  width:88px;
  height:88px;
  margin:0 auto 28px;
}
#accountModal .account-modal-title{
  margin:0 0 14px;
  line-height:1.1;
}
#accountModal .account-modal-subtitle{
  max-width:420px;
  margin:0 auto 30px;
  line-height:1.45;
}
#accountModal .account-modal-actions{
  display:flex;
  flex-direction:column;
  align-items:center;
  gap:12px;
}
#accountModal .account-modal-button{
  width:min(100%,390px);
  min-height:50px;
  margin:0;
  padding:13px 20px;
}

@media(max-width:700px){
  .modal-close,
  .account-modal-close{
    top:14px;
    right:14px;
    width:38px;
    height:38px;
  }

  .modal-close span,
  .account-modal-close span{
    top:17px;
    height:4px;
  }

  .modal-close span:nth-child(1),
  .account-modal-close span:nth-child(1){
    top:10px;
  }

  .modal-close span:nth-child(3),
  .account-modal-close span:nth-child(3){
    left:18px;
    top:24px;
  }

  #accountModal .product-modal-body{
    padding:34px 24px 28px;
  }

  #accountModal .account-modal-avatar{
    width:80px;
    height:80px;
    margin-bottom:24px;
  }

  #accountModal .account-modal-subtitle{
    margin-bottom:26px;
  }

  #accountModal .account-modal-actions{
    gap:10px;
  }

  #accountModal .account-modal-button{
    width:100%;
  }
}
